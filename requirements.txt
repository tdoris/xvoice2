# Runtime dependencies. Lower bounds chosen so wheels exist on modern Pythons
# (incl. 3.12); avoid exact pins that force source builds.
pyaudio>=0.2.14
numpy>=1.26
requests>=2.31

# Optional: Parakeet (NVIDIA) transcription engine. Only needed when
# TRANSCRIPTION_ENGINE = "parakeet". Install via: pip install -e .[parakeet]
# onnx-asr
# onnxruntime
# huggingface_hub

# Test dependencies live in the [test] extra (see setup.py):
#   pip install -e .[test]
